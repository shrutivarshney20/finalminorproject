<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login Form</title>
    <!-- Include Tailwind CSS via CDN -->
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
    <!-- Include Font Awesome CSS -->
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.4/css/all.min.css" rel="stylesheet">

    <style>
        /* Additional styles */
        .social-icon {
            width: 80px;
            height: 80px;
            border-radius: 50%;
            position: relative;
            overflow: hidden;
        }

        .water {
            position: absolute;
            bottom: 0;
            left: 0;
            width: 100%;
            height: 0;
            background-color: #3182ce;
            transition: height 0.3s ease-in-out;
        }

        .social-icon:hover .water {
            height: 80%;
        }

        /* Larger Font Awesome icons */
        .social-icon i {
            font-size: 20px;
            color: #3182ce;
            transition: color 0.3s ease-in-out;
        }

        /* Change color to white when water fills */
        .social-icon:hover i {
            color: white;
        }
    </style>
</head>
<body class="flex justify-center items-center h-screen" style="background-image: url('https://github.com/garvit31602/minor-html/blob/main/login1.jpg?raw=true'); background-size: cover; background-position: center;">
    <div class="w-full max-w-xs border-2 border-white rounded-md p-6 bg-white opacity-80">
        <h1 class="text-2xl font-bold mb-4 text-center">Login Form</h1>

        <form action="/login" method="post" class="mb-4">
            <input placeholder="Name" name="name" type="text" class="border p-2 rounded-md mb-2 w-full">
            <br>
            <input placeholder="Password" name="password" type="password" class="border p-2 rounded-md mb-2 w-full">
            <br>
            <input class="bg-blue-700 text-white px-4 py-2 rounded-md cursor-pointer w-full" type="submit">
            <h3 class="mt-4 text-center">Don't have an account?</h3>
            <a href="/signup" class="text-blue-500 block text-center">Create a new account</a>
        </form>
    </div>
</body>
</html>
